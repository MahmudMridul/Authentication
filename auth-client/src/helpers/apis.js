const base = "https://localhost:9001/api/Auth";

export const signup = `${base}/signup`;
export const signin = `${base}/signin`;
export const signout = `${base}/signout`;
export const isAuth = `${base}/is-authenticated`;
